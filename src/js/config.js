export const minScale = 0.60;
export const scaleMultiplier = 100;

export const shape = {
  stroke: 10,
  glow: 20,
};

export const colors = {
  pink: `#ff69b4`,
  bg: `black`,
};

export const $canvas = document.querySelector(`.canvas`);
export const $notificationBounds = document.querySelector(`.notification-bounds`);
export const $notificationScale = document.querySelector(`.notification-scale`);
export const $alert = document.querySelector(`.circle-is-placed`);
